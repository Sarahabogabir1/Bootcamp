var decode = require('./decoder.js')
//want to export the function decode; 
//like ruby require relative

function super_decoder(sentence,behavior) {
	var behavior = behavior.split("-"); //split with hyphen
	behaviors = ["even", "backwards"];
}
